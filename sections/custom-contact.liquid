{% comment %} CONTACT MODAL {% endcomment %}
 <section class="x:absolute x:h-screen x:w-screen x:bg-black/50 x:flex x:justify-center x:items-center x:z-50" hidden id="test">
  <div class="x:w-[35rem] x:h-[20rem] x:bg-white x:rounded-lg x:p-10 x:flex x:flex-col x:items-center x:justify-between">
    <div class="x:flex x:items-center x:flex-col">
        <input type="checkbox" checked class="x:accent-black x:h-10 x:w-10">
        <h2 class="x:uppercase x:font-normal">message sent successfully</h2>
    </div>

    <p class="x:text-center">
      Thank you for reaching out. Our team will review your message and get back to you within 2-3 business days. We
      appreciate your patience.
    </p>


    <div class="x:flex x:items-center x:justify-center x:gap-5 x:w-full">
        <button class="x:capitalize x:border-black x:border-2 x:py-1 x:px-2 x:hover:scale-110 x:duration-300 x:transition-all">back to home</button>
        <button class="x:capitalize x:bg-black x:text-white x:py-1 x:px-2 x:hover:scale-110 x:duration-300 x:transition-all">continue shopping</button>
    </div>
  </div>
</section>

<section class="x:flex x:flex-col x:items-center x:border-t-2">
  {% comment %} CONTACT US SECTION {% endcomment %}
  <div class="container x:w-[70rem] x:flex x:flex-col x:gap-5">
    <div class="section x:flex x:flex-col x:gap-5">
      <div class="x:flex x:justify-center x:flex-col x:items-center x:mb-[6rem]">
        <h1 class="x:uppercase">need help? send us a message</h1>
        <span>To get help with your online purchase, contact us at 718-854-2595 or use the form below.</span>
      </div>

      <div class="x:flex x:flex-col x:items-center x:w-full x:gap-5">
        {% comment %} <form> {% endcomment %}
            {% comment %} FIRST NAME & LAST NAME INPUTS {% endcomment %}
            <div class="x:flex x:gap-5 x:w-full">
              {% comment %} FIRST NAME INPUT {% endcomment %}
              <div class="x:relative x:group x:w-full">
                <input placeholder="First Name" class="x:pl-12 x:py-5 x:pr-5 x:border-2 x:border-black x:text-xl x:w-full">
    
                <div class="x:absolute x:flex x:items-center x:left-3 x:top-1/2 x:-translate-y-1/2 x:gap-3">
                  <i>
                    {%- render 'icon' with 'user', class: 'user' -%}
                  </i>
                </div>
              </div>
    
              {% comment %} LAST NAME INPUT {% endcomment %}
              <div class="x:relative x:group x:w-full">
                <input placeholder="Last Name" class="x:pl-12 x:py-5 x:pr-5 x:border-2 x:border-black x:text-xl x:w-full">
    
                <div class="x:absolute x:flex x:items-center x:left-3 x:top-1/2 x:-translate-y-1/2 x:gap-3">
                  <i>
                    {%- render 'icon' with 'user', class: 'user' -%}
                  </i>
                </div>
              </div>
            </div>
    
            {% comment %} EMAIL INPUT {% endcomment %}
            <div class="x:w-full">
              <div class="x:relative x:group x:w-full">
                <input placeholder="Email" class="x:pl-12 x:py-5 x:pr-5 x:border-2 x:border-black x:text-xl x:w-full">
    
                <div class="x:absolute x:flex x:items-center x:left-3 x:top-1/2 x:-translate-y-1/2 x:gap-3">
                  <i>
                    {%- render 'icon' with 'email', class: 'email' -%}
                  </i>
                </div>
              </div>
            </div>
    
            {% comment %} REASON INPUT SECTION {% endcomment %}
            <div class="x:w-full">
              <div class="x:border-2 x:border-black x:p-5 x:flex x:flex-col x:gap-5">
                {% comment %} REASON HEADER {% endcomment %}
                <div class="x:flex x:items-center x:gap-3">
                  {%- render 'icon' with 'question-circle' -%}
                  <label class="x:capitalize">reason for contacting</label>
                </div>
    
                <div class="x:flex x:flex-col x:ml-5">
                  {% comment %} SHIPPING CHECKBOX {% endcomment %}
                  <div class="x:flex x:items-center x:gap-3">
                    <input type="checkbox" class="x:w-6 x:h-6 x:accent-black">
                    <label class="x:capitalize x:text-xl">shipping</label>
                  </div>
    
                  {% comment %} PRODUCT CHECKBOX {% endcomment %}
                  <div class="x:flex x:items-center x:gap-3">
                    <input type="checkbox" class="x:w-6 x:h-6 x:accent-black">
                    <label class="x:capitalize x:text-xl">Product</label>
                  </div>
    
                  {% comment %} OTHERS CHECKBOX WITH INPUT {% endcomment %}
                  <div class="x:flex x:items-center x:gap-3">
                    <input type="checkbox" class="x:w-6 x:h-6 x:accent-black">
                    <div class="x:flex x:items-center x:gap-3">
                      <label class="x:capitalize x:text-xl">others:</label>
                      <input placeholder="Please specify..." class="x:border-b-2 x:border-black">
                    </div>
                  </div>
                </div>
              </div>
            </div>
    
            {% comment %} MESSAGE TEXT AREA SECTION {% endcomment %}
            <div class="x:w-full x:border-2 x:border-black x:p-5">
              <div class="x:relative">
                <i class="x:absolute">
                  {%- render 'icon' with 'message' -%}
                </i>
    
                <textarea
                  placeholder="Type Your Message Below"
                  class="x:w-full x:pl-10 x:pr-4 x:pb-4 x:text-base x:resize-none x:outline-none"
                  rows="4"
                ></textarea>
              </div>
            </div>
    
            <div class="x:w-full">
              <button id="contact-submit" class="x:w-full x:border x:border-black x:bg-[#222222] x:text-white x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:h-[3.75rem] x:uppercase" type="submit">
                submit
              </button>
            </div>
        {% comment %} </form> {% endcomment %}
      </div>
    </div>
  </div>

  {% comment %} FAQ SECTION {% endcomment %}
  <!-- <div class="container x:border-t-2 x:w-full">
    <div class="section x:flex x:flex-col x:gap-5">
      {% comment %} FAQ HEADER {% endcomment %}
      <div class="x:flex x:justify-center x:flex-col x:items-center x:mb-[6rem]">
        <h1 class="x:uppercase">frequently asked questions</h1>
        <span>Find quick answers to common questions about your order, shipping, returns, and more.</span>
      </div>

      {% comment %} FAQ BUTTONS SECTION {% endcomment %}
      <div class="x:flex x:w-full x:justify-between x:my-5">
        <button class="x:border x:border-black x:text-black x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:w-[21.875rem] x:h-[3.75rem] x:uppercase">
          orders
        </button>
        <button class="x:border x:border-black x:text-black x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:w-[21.875rem] x:h-[3.75rem] x:uppercase">
          returns
        </button>
        <button class="x:border x:border-black x:text-black x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:w-[21.875rem] x:h-[3.75rem] x:uppercase">
          pick-up
        </button>
        <button class="x:border x:border-black x:text-black x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:w-[21.875rem] x:h-[3.75rem] x:uppercase">
          payment
        </button>
      </div>

      {% comment %} FAQ SELECTION SECTION {% endcomment %}
      <div class="x:flex x:flex-col x:gap-5">
        <div class="x:border-2 x:border-black x:px-3 x:py-4 x:flex x:justify-between x:items-center x:group x:transition-all x:duration-300 x:hover:cursor-pointer">
          <label class="x:group-hover:ml-5 x:transition-all x:duration-300">How do I place an order?</label>
          <i class="x:group-hover:scale-125 x:transition-all x:duration-300">
            {%- render 'icon' with 'plus' -%}
          </i>
        </div>

        <div class="x:border-2 x:border-black x:px-3 x:py-4 x:flex x:justify-between x:items-center x:group x:transition-all x:duration-300 x:hover:cursor-pointer">
          <label class="x:group-hover:ml-5 x:transition-all x:duration-300">Where can I track my orders?</label>
          <i class="x:group-hover:scale-125 x:transition-all x:duration-300">
            {%- render 'icon' with 'plus' -%}
          </i>
        </div>

        <div class="x:border-2 x:border-black x:px-3 x:py-4 x:flex x:flex-col x:justify-between x:group x:transition-all x:duration-300 x:hover:cursor-pointer ">
          <div class="x:flex x:justify-between x:items-center x:w-full">
            <label class="x:group-hover:ml-5 x:transition-all x:duration-300 x:font-bold"
              >When will my order be processed and shipped?</label
            >
            <i class="x:group-hover:scale-125 x:transition-all x:duration-300">
              {%- render 'icon' with 'minus' -%}
            </i>
          </div>

          <p class="">
            We aim to process all orders within 1–2 business days. You will receive a confirmation email with tracking
            details once your order has shipped.
          </p>
        </div>

        <div class="x:border-2 x:border-black x:px-3 x:py-4 x:flex x:justify-between x:items-center x:group x:transition-all x:duration-300 x:hover:cursor-pointer">
          <label class="x:group-hover:ml-5 x:transition-all x:duration-300"
            >Can I make changes to my order after placing it?</label
          >
          <i class="x:group-hover:scale-125 x:transition-all x:duration-300">
            {%- render 'icon' with 'plus' -%}
          </i>
        </div>

        <div class="x:border-2 x:border-black x:px-3 x:py-4 x:flex x:justify-between x:items-center x:group x:transition-all x:duration-300 x:hover:cursor-pointer">
          <label class="x:group-hover:ml-5 x:transition-all x:duration-300"
            >Are all the Closeout Connection products posted available in physical stores?</label
          >
          <i class="x:group-hover:scale-125 x:transition-all x:duration-300">
            {%- render 'icon' with 'plus' -%}
          </i>
        </div>
      </div>
    </div>
  </div> -->
<div class="x:flex x:flex-col x:gap-8">
  <!-- Tab Buttons -->
  <div class="x:flex x:flex-wrap x:justify-start x:gap-4">
    {% assign categories = section.blocks | map: 'settings.category' | uniq %}
    <button class="faq-tab x-border x-border-black x-px-6 x-py-2 x-uppercase x-font-medium x-transition-all x-duration-300" data-category="all">All</button>
    {% for category in categories %}
      {% if category != blank %}
        <button class="faq-tab x-border x-border-black x-px-6 x-py-2 x-uppercase x-font-medium x-transition-all x-duration-300" data-category="{{ category | escape }}">
          {{ category }}
        </button>
      {% endif %}
    {% endfor %}
  </div>

  <!-- FAQ List -->
  <div class="x:flex x:flex-col x:gap-5" id="faq-list">
    {% for block in section.blocks %}
      <div class="faq-item x-border-2 x-border-black x-px-3 x-py-4 x-transition-all x-duration-300" data-category="{{ block.settings.category | escape }}">
        <button type="button" class="faq-toggle x-flex x-justify-between x-items-center x-w-full x-group x-hover:cursor-pointer" aria-expanded="false">
          <label class="x-group-hover:ml-5 x-transition-all x-duration-300 x-font-medium">
            {{ block.settings.question }}
          </label>
          <i class="faq-icon x-transition-transform x-duration-300">
            {%- render 'icon' with 'plus' -%}
          </i>
        </button>
        <div class="faq-answer x-mt-2 x-hidden">
          <p>{{ block.settings.answer }}</p>
        </div>
      </div>
    {% endfor %}
  </div>
</div>
  
  {% comment %} BOTTOM BANNER {% endcomment %}
  <div class="x:h-[7rem] x:bg-[#222222] x:w-full x:flex x:justify-center x:items-center x:gap-10 x:text-white">
    <div class="x:flex x:flex-col">
      <h2 class="x:uppercase">free shipping on orders over $200</h2>
      <span>Get alerts about new items, sales, and more.</span>
    </div>

    <button class="x:border x:border-white x:text-white x:text-lg x:tracking-wider x:flex x:items-center x:justify-center x:hover:bg-white x:hover:text-black x:transition x:w-[21.875rem] x:h-[3.75rem] x:uppercase">
      get started
    </button>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    document.querySelectorAll(".faq-toggle").forEach(function (toggleBtn) {
      toggleBtn.addEventListener("click", function () {
        const parent = toggleBtn.closest(".faq-item");
        const answer = parent.querySelector(".faq-answer");
        const icon = toggleBtn.querySelector(".faq-icon");
        const isOpen = !answer.classList.contains("x-hidden");

        if (isOpen) {
          answer.classList.add("x-hidden");
          icon.innerHTML = `{%- render 'icon' with 'plus' -%}`;
        } else {
          answer.classList.remove("x-hidden");
          icon.innerHTML = `{%- render 'icon' with 'minus' -%}`;
        }
      });
    });

    document.querySelectorAll(".faq-tab").forEach(function (tabBtn) {
      tabBtn.addEventListener("click", function () {
        const category = tabBtn.dataset.category;
        document.querySelectorAll(".faq-item").forEach(function (item) {
          if (category === "all" || item.dataset.category === category) {
            item.style.display = "block";
          } else {
            item.style.display = "none";
          }
        });
      });
    });
  });
</script>

{% schema %}
{
  "name": "FAQ Section",
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "category",
          "label": "Category (e.g., Orders, Returns)",
          "default": "General"
        },
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "How do I place an order?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "You can place an order by adding items to your cart and proceeding to checkout."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Dynamic FAQ with Categories",
      "blocks": [
        {
          "type": "faq_item"
        },
        {
          "type": "faq_item"
        }
      ]
    }
  ]
}
{% endschema %}

{% comment %} <script>
    $(document).ready(function () {
      $("#contact-submit").on("click", function (e) {
        e.preventDefault();
        $("#test").removeAttr("hidden");
      });
    });
</script> {% endcomment %}
  